// && -> AND Gate
// || -> OR Gate

let a = true;
let b = false;
console.log(a && b); //  AND
console.log(a || b); //  OR
console.log(!a); //  NOT

console.log(5 != "g");  // Value 5, g ! that true, but 5 !== "g" is also true, because they are different types.